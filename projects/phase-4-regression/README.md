# Phase 4 实战项目：房价预测（回归任务）

## 📋 项目概述

使用多层感知机 (MLP) 预测房屋价格，综合运用 Phase 4 所学的神经网络基础知识。

## 🎯 学习目标

- 全连接网络在回归任务中的应用
- MSE 损失函数和回归评估指标
- 正则化技术实践（Dropout、BatchNorm、L2）
- 权重初始化（He 初始化）
- 特征标准化的重要性

## 📊 数据集

**California Housing Dataset**（Scikit-learn 内置）

- 样本数：~20,000
- 特征数：8（收入、房龄、房间数等）
- 目标：房价中位数（单位：$100,000）

## 🏗️ 模型架构

```
输入: (8,)  # 8 个房屋特征
    ↓
Linear(8→128) + BatchNorm + ReLU + Dropout(0.2)
    ↓
Linear(128→64) + BatchNorm + ReLU + Dropout(0.2)
    ↓
Linear(64→32) + ReLU
    ↓
Linear(32→1)  # 无激活，线性输出
    ↓
输出: (1,)  # 预测价格
```

## 🚀 运行方式

```bash
# 进入项目目录
cd projects/phase-4-regression

# 激活虚拟环境
source ../../.venv/bin/activate

# 运行项目
python house_price_mlp.py
```

## 📁 生成文件

| 文件                             | 说明              |
| -------------------------------- | ----------------- |
| `outputs/training_curves.png`    | 训练/验证损失曲线 |
| `outputs/predictions.png`        | 预测 vs 真实值    |
| `outputs/feature_importance.png` | 特征重要性分析    |
| `outputs/residuals.png`          | 残差分析          |
| `outputs/best_model.pth`         | 最佳模型权重      |

## 📈 预期结果

- 测试集 R² > 0.6
- 测试集 RMSE < 0.8
- 训练时间: ~30 秒

## ✅ 核心知识点

### 回归 vs 分类

| 对比项     | 分类任务        | 回归任务      |
| ---------- | --------------- | ------------- |
| 输出层激活 | Softmax/Sigmoid | 无（线性）    |
| 损失函数   | CrossEntropy    | MSE/MAE       |
| 评估指标   | Accuracy, F1    | RMSE, MAE, R² |

### 特征标准化

```python
# 回归任务中特征标准化非常重要
scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

### L2 正则化 (Weight Decay)

```python
optimizer = optim.Adam(model.parameters(), lr=0.001, weight_decay=1e-4)
```
