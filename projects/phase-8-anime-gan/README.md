# Phase 8 实战项目：DCGAN 动漫人脸生成

> **目标**：使用 DCGAN 生成动漫风格的人脸图像  
> **技术栈**：PyTorch, DCGAN  
> **预计训练时间**：GPU 约 30-60 分钟，CPU 约数小时

---

## 🎯 学习目标

1. 实践 DCGAN 的完整训练流程
2. 掌握生成对抗网络的调参技巧
3. 理解生成质量与训练时间的权衡
4. 能够生成可辨认的动漫人脸图像

---

## 📁 项目结构

```
phase-8-anime-gan/
├── README.md           # 项目说明（本文件）
├── models.py           # DCGAN 模型定义
├── utils.py            # 工具函数
├── train.py            # 训练脚本
├── generate.py         # 生成图像脚本
├── data/               # 数据目录
└── outputs/
    ├── checkpoints/    # 模型检查点
    └── samples/        # 生成的样本
```

---

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install torch torchvision matplotlib pillow tqdm
```

### 2. 快速演示（使用随机数据）

```bash
# 不需要真实数据，使用随机噪声进行 Demo 训练
python train.py --demo --epochs 5
```

### 3. 使用真实数据训练

#### 3.1 下载数据集

推荐使用 [Anime Face Dataset](https://www.kaggle.com/datasets/splcher/animefacedataset):

```bash
# 方法 1: 使用 Kaggle CLI
pip install kaggle
kaggle datasets download -d splcher/animefacedataset
unzip animefacedataset.zip -d data/

# 方法 2: 手动下载
# 从 Kaggle 下载后解压到 data/ 目录
```

#### 3.2 开始训练

```bash
# 训练（使用 GPU）
python train.py --data_dir data/images --epochs 50 --batch_size 64

# 训练（使用 CPU，较慢）
python train.py --data_dir data/images --epochs 20 --batch_size 32
```

### 4. 生成图像

```bash
# 生成 16 张图像
python generate.py --checkpoint outputs/checkpoints/latest.pth --num_images 16

# 生成隐空间插值动画
python generate.py --checkpoint outputs/checkpoints/latest.pth --interpolate
```

---

## 📊 训练参数说明

| 参数           | 默认值        | 说明                 |
| -------------- | ------------- | -------------------- |
| `--data_dir`   | `data/images` | 图像数据目录         |
| `--batch_size` | 64            | 批次大小             |
| `--epochs`     | 50            | 训练轮数             |
| `--lr`         | 0.0002        | 学习率               |
| `--latent_dim` | 100           | 噪声向量维度         |
| `--image_size` | 64            | 生成图像尺寸         |
| `--demo`       | False         | 使用随机数据快速演示 |

---

## 📈 预期效果

| 训练阶段 | Epoch | 效果描述       |
| -------- | ----- | -------------- |
| 早期     | 1-5   | 模糊的彩色噪声 |
| 中期     | 10-20 | 可见脸部轮廓   |
| 后期     | 30-50 | 清晰的动漫人脸 |

---

## 🔧 常见问题

### Q: 判别器损失快速下降到 0 怎么办？

**A**: 判别器太强，尝试：

- 减小判别器学习率
- 增加标签平滑（在代码中已启用）
- 减少判别器训练步数

### Q: 生成图像模糊或全黑？

**A**: 训练不足或模式崩塌，尝试：

- 增加训练轮数
- 检查数据加载是否正确
- 调整学习率

### Q: 显存不足？

**A**: 减小 batch_size 或 image_size

---

## 📚 相关知识

- [01-gan-basics.py](../../src/phase-8-generative/01-gan-basics.py) - GAN 基础
- [02-dcgan.py](../../src/phase-8-generative/02-dcgan.py) - DCGAN 架构

---

## ✅ 完成标准

- [ ] 成功运行 demo 模式
- [ ] 在真实数据集上训练模型
- [ ] 生成可辨认的动漫人脸
- [ ] 理解 GAN 训练的调参过程
