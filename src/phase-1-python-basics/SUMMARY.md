# Phase 1: Python 数据科学基础 - 学习总结

> **完成日期**：2025-12-11  
> **学习时长**：约 1 周

---

## 🎯 学习目标回顾

完成本阶段后，你已经掌握了：

1. ✅ 使用 NumPy 进行高效的数值计算
2. ✅ 使用 Pandas 处理和分析结构化数据
3. ✅ 使用 Matplotlib 创建专业的数据可视化
4. ✅ 完成完整的探索性数据分析 (EDA)

---

## 📚 核心知识点总结

### 1. NumPy 基础 (`01-numpy-basics.py`)

| 主题     | 关键知识点                                                                                   |
| -------- | -------------------------------------------------------------------------------------------- |
| 数组创建 | `np.array()`, `np.zeros()`, `np.ones()`, `np.arange()`, `np.linspace()`, `np.random.randn()` |
| 数组属性 | `shape`, `ndim`, `size`, `dtype`                                                             |
| 数组索引 | 基本索引 `arr[i]`, 二维索引 `arr[i, j]`, 切片 `arr[start:end]`                               |
| 布尔索引 | `arr[arr > 3]`, `arr[(arr > 2) & (arr < 7)]`                                                 |
| 条件处理 | `np.where(condition, true_val, false_val)`                                                   |

**核心示例**：

```python
arr = np.arange(12).reshape(3, 4)  # 创建并重塑数组
arr[arr > 5]                        # 布尔索引筛选
np.where(arr > 5, 1, 0)             # 条件赋值
```

---

### 2. NumPy 运算与广播 (`02-numpy-operations.py`)

| 主题       | 关键知识点                                                  |
| ---------- | ----------------------------------------------------------- |
| 元素级运算 | `+`, `-`, `*`, `/`, `**` (逐元素操作)                       |
| 矩阵乘法   | `@` 或 `np.matmul()` 或 `np.dot()`                          |
| 广播机制   | 形状兼容时自动扩展维度进行运算                              |
| 聚合函数   | `np.sum()`, `np.mean()`, `np.max()`, `np.min()`, `np.std()` |
| 按轴聚合   | `axis=0` (按列), `axis=1` (按行)                            |
| 数学函数   | `np.sin()`, `np.cos()`, `np.exp()`, `np.log()`, `np.sqrt()` |

**广播规则**：

1. 维度数不同时，在较小数组前面补 1
2. 某维度大小为 1 时，沿该维度扩展
3. 维度大小不同且都不为 1 时报错

**向量化优势**：比 Python 循环快 **100-1000 倍**

---

### 3. NumPy 线性代数 (`03-numpy-linear-algebra.py`)

| 主题       | 关键知识点                                                       |
| ---------- | ---------------------------------------------------------------- |
| 矩阵运算   | `A.T` (转置), `np.linalg.inv()` (逆), `np.linalg.det()` (行列式) |
| 线性方程组 | `np.linalg.solve(A, b)`                                          |
| 特征分解   | `np.linalg.eig()` → 特征值和特征向量                             |
| 奇异值分解 | `np.linalg.svd()` → U, Σ, V^T                                    |
| 范数       | `np.linalg.norm(v, ord=1/2/np.inf)`                              |

**深度学习相关应用**：

- **PCA 降维**：特征分解协方差矩阵
- **模型压缩**：SVD 低秩近似
- **正则化**：L1/L2 范数防止过拟合

---

### 4. Pandas 基础 (`04-pandas-basics.py`)

| 主题     | 关键知识点                                                    |
| -------- | ------------------------------------------------------------- |
| 数据结构 | `DataFrame` (二维表格), `Series` (一维数组)                   |
| 基本属性 | `shape`, `columns`, `index`, `dtypes`, `describe()`, `info()` |
| 列操作   | `df['col']`, `df[['col1', 'col2']]`, 添加/删除列              |
| 行索引   | `loc` (标签, 包含结束), `iloc` (位置, 不包含结束)             |
| 条件筛选 | `df[df['col'] > val]`, `df.query('col > val')`, `df.isin()`   |
| 排序     | `df.sort_values()`                                            |
| 分组聚合 | `df.groupby('col').agg(['mean', 'sum', 'count'])`             |

---

### 5. Pandas 数据预处理 (`05-pandas-preprocessing.py`)

| 主题       | 关键知识点                                                                     |
| ---------- | ------------------------------------------------------------------------------ |
| 缺失值检测 | `df.isnull().sum()`, `df.isnull().mean()`                                      |
| 缺失值填充 | `fillna(value)`, `fillna(method='ffill')`, 均值/中位数/众数                    |
| 缺失值删除 | `dropna()`, `dropna(thresh=n)`                                                 |
| 类型转换   | `astype()`, `pd.to_datetime()`                                                 |
| 字符串处理 | `str.strip()`, `str.lower()`, `str.split()`, `str.contains()`, `str.replace()` |
| 重复值处理 | `duplicated()`, `drop_duplicates()`                                            |
| 异常值检测 | IQR 方法, Z-score 方法                                                         |
| 数据合并   | `pd.merge()` - inner/left/right/outer join                                     |
| 透视表     | `pd.pivot_table()`                                                             |

---

### 6. Matplotlib 基础 (`06-matplotlib-basics.py`)

| 图表类型 | 函数             | 适用场景           |
| -------- | ---------------- | ------------------ |
| 折线图   | `plt.plot()`     | 趋势变化、时间序列 |
| 散点图   | `plt.scatter()`  | 相关性、分布       |
| 柱状图   | `plt.bar()`      | 类别比较           |
| 直方图   | `plt.hist()`     | 数据分布           |
| 多子图   | `plt.subplots()` | 组合展示           |

**常用配置**：

```python
plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']  # 中文支持
plt.rcParams['axes.unicode_minus'] = False              # 负号显示
```

---

### 7. 高级可视化 (`07-visualization-advanced.py`)

| 主题       | 关键知识点                                    |
| ---------- | --------------------------------------------- |
| 多子图布局 | `plt.subplot()`, `plt.subplots()`, `GridSpec` |
| 自定义样式 | 颜色、线型、字体、样式主题                    |
| 双轴图表   | `ax.twinx()`                                  |
| 热力图     | `ax.imshow()` + 颜色条                        |
| 箱线图     | `ax.boxplot()` - 显示四分位数、异常值         |
| 小提琴图   | `ax.violinplot()` - 显示分布形状              |
| 堆叠图     | `plt.stackplot()`, 堆叠柱状图                 |
| 3D 可视化  | `Axes3D`, `plot_surface()`, 3D 散点图         |
| Seaborn    | `regplot`, `kdeplot`, `pairplot`              |

---

## 🔑 关键公式与概念

### 广播规则示意

```
(2, 3) + (3,)   → (2, 3) + (1, 3) → (2, 3)  ✅
(4, 1) + (3,)   → (4, 1) + (1, 3) → (4, 3)  ✅
(4, 3) + (3, 4) → 形状不兼容                  ❌
```

### Softmax 函数（数值稳定版）

```python
def softmax(x):
    x_shifted = x - np.max(x)  # 防止 exp 溢出
    exp_x = np.exp(x_shifted)
    return exp_x / np.sum(exp_x)
```

### PCA 降维步骤

1. 数据中心化（减去均值）
2. 计算协方差矩阵
3. 特征分解协方差矩阵
4. 选择前 k 个最大特征值对应的特征向量
5. 将数据投影到这些特征向量上

### 异常值检测 - IQR 方法

```python
Q1 = df['col'].quantile(0.25)
Q3 = df['col'].quantile(0.75)
IQR = Q3 - Q1
lower = Q1 - 1.5 * IQR
upper = Q3 + 1.5 * IQR
outliers = df[(df['col'] < lower) | (df['col'] > upper)]
```

---

## 💡 深度学习预备知识

本阶段为后续深度学习学习打下了重要基础：

| 本阶段技能      | 深度学习应用                   |
| --------------- | ------------------------------ |
| NumPy 数组操作  | 张量运算、批处理数据           |
| 广播机制        | 批量归一化、特征缩放           |
| 线性代数        | 全连接层、注意力机制           |
| 特征分解/SVD    | PCA 降维、LoRA 微调            |
| 范数            | L1/L2 正则化、梯度裁剪         |
| Pandas 数据处理 | 数据预处理、特征工程           |
| 可视化          | 训练曲线、混淆矩阵、特征可视化 |

---

## 📊 技能自检清单

- [x] 能够创建和操作多维数组
- [x] 理解广播机制
- [x] 能够进行基本的线性代数运算
- [x] 能够读取和清洗 CSV 数据
- [x] 能够创建多种类型的图表
- [x] 理解数据预处理流程

---

## 🚀 下一步

恭喜完成 Phase 1！现在可以进入 **Phase 2: 深度学习数学基础**，学习：

- 微积分（梯度、链式法则）
- 概率统计（概率分布、贝叶斯定理）
- 优化理论（梯度下降）
